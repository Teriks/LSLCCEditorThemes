Themes can be added to the editor by opening `settings -> Editor Theme`

You create a new blank theme by clicking "new", and import a theme file
over the newly created theme using the "import" button.